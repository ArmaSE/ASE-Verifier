// File for GET funcitons of Express
exports.run = (client, request, response, conf) => {
    var refuse = `You are not authorised to use this module`;
    var refuseError = "insufficientPerms";
    var errorType, causeMessage, doneMessage;

    
}